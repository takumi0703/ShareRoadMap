<div class="container g-container">
    <div class="row g-form">
        <%= form_with url: road_new_path do |form| %>
            <h1>目標を設定する！</h1>
            <div class="g-input">
                <%= form.text_field :goal, :class =>"g-input"  %>
                <%= form.submit :class =>"g-btn", :value =>"作成する"  %>
            </div>
        <% end %>
    </div>
</div>
